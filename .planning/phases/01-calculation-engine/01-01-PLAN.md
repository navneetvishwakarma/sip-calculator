---
phase: 01-calculation-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - turbo.json
  - pnpm-workspace.yaml
  - package.json
  - .npmrc
  - apps/web/package.json
  - apps/web/next.config.ts
  - apps/web/tsconfig.json
  - apps/web/app/page.tsx
autonomous: true
requirements:
  - CALC-01
  - CALC-02
  - CALC-03
  - CALC-04
  - CALC-05
  - CALC-06

must_haves:
  truths:
    - "Running `pnpm install` from the repo root completes without error"
    - "`pnpm-workspace.yaml` declares both apps/* and packages/* workspaces"
    - "`turbo.json` uses the Turbo 2.x `tasks` key (not the Turbo 1.x `pipeline` key)"
    - "`apps/web` has a `next dev` bootable skeleton with zero application pages"
    - "`apps/web/package.json` lists `@sip/core: workspace:*` as a dependency (Phase 2 import readiness)"
  artifacts:
    - path: "turbo.json"
      provides: "Turbo 2.x task pipeline"
      contains: '"tasks"'
    - path: "pnpm-workspace.yaml"
      provides: "Workspace declarations"
      contains: "apps/*"
    - path: "package.json"
      provides: "Root manifest with pnpm packageManager field"
      contains: '"packageManager"'
    - path: "apps/web/package.json"
      provides: "Next.js 15 app manifest"
      contains: '"@sip/core": "workspace:*"'
    - path: "apps/web/app/page.tsx"
      provides: "Minimal App Router entry point (empty div)"
  key_links:
    - from: "apps/web/package.json"
      to: "packages/core (future)"
      via: "workspace:* dependency"
      pattern: '"@sip/core": "workspace:\*"'
---

<objective>
Bootstrap the Turborepo monorepo root and create an `apps/web` stub so Phase 2 can `import from "@sip/core"` on day one.

Purpose: Establish workspace structure, Turbo task pipeline, and the minimum Next.js 15 shell that Phase 2 will populate. No application code or calculator logic in this plan.

Output: Root config files (turbo.json, pnpm-workspace.yaml, package.json, .npmrc) and a bootable but empty apps/web skeleton (package.json, next.config.ts, tsconfig.json, app/page.tsx).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-calculation-engine/01-CONTEXT.md
@.planning/phases/01-calculation-engine/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Monorepo root config files</name>
  <files>turbo.json, pnpm-workspace.yaml, package.json, .npmrc</files>
  <read_first>
    - .planning/phases/01-calculation-engine/01-RESEARCH.md (Finding 1 lines 269–329, Finding 10 lines 660–683 — exact file contents specified)
    - .planning/phases/01-calculation-engine/01-CONTEXT.md (D-01 — pnpm, Turborepo, @sip/core)
  </read_first>
  <action>
Create four root-level files from scratch. Do NOT run `pnpm dlx create-turbo@latest` — author directly from the research specs for determinism.

**turbo.json** — Turbo 2.x syntax (`"tasks"`, NOT `"pipeline"`):
```json
{
  "$schema": "https://turborepo.dev/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "test": {
      "outputs": ["coverage/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
```
Note: `test` does NOT depend on `build` — Vitest runs against TypeScript source via esbuild directly. Verify: the key is `"tasks"` not `"pipeline"`.

**pnpm-workspace.yaml**:
```yaml
packages:
  - "apps/*"
  - "packages/*"
```

**package.json** (root):
```json
{
  "name": "sip-calculator",
  "private": true,
  "packageManager": "pnpm@10.10.0",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "2.9.9"
  }
}
```
Confirm the installed pnpm version by running `pnpm --version` before writing; update the `packageManager` field if the actual version differs. Use exact versions (no `^` or `~`) per security constraint — supply chain pinning.

**.npmrc**:
```
auto-install-peers=true
strict-peer-dependencies=false
```
  </action>
  <verify>
    <automated>cd /c/Users/navne.FUDGE/source/sip-calculator && node -e "const t=require('./turbo.json'); if(!t.tasks) throw new Error('turbo.json missing tasks key'); console.log('turbo.json OK')"</automated>
  </verify>
  <acceptance_criteria>
    - turbo.json contains `"tasks"` key (not `"pipeline"`)
    - pnpm-workspace.yaml contains `"apps/*"` and `"packages/*"`
    - package.json contains `"packageManager"` field with exact pnpm version
    - package.json devDependencies contains `"turbo": "2.9.9"` (exact, no caret)
    - .npmrc file exists at repo root
  </acceptance_criteria>
  <done>Four root config files authored. `node -e "require('./turbo.json').tasks"` returns the task object without throwing.</done>
</task>

<task type="auto">
  <name>Task 2: apps/web minimal stub (per D-01 and OQ-3)</name>
  <files>apps/web/package.json, apps/web/next.config.ts, apps/web/tsconfig.json, apps/web/app/page.tsx, apps/web/app/layout.tsx</files>
  <read_first>
    - .planning/phases/01-calculation-engine/01-CONTEXT.md (D-01 — stub depth, Phase 2 import readiness)
    - .planning/phases/01-calculation-engine/01-RESEARCH.md (Finding 1 lines 287–292 — what to strip; line 514 — apps/web package.json must have workspace:*)
    - .planning/phases/01-calculation-engine/01-PATTERNS.md (apps/web files in dependency graph)
  </read_first>
  <action>
OQ-3 resolved: minimal 4-file stub only. Do NOT run `create-next-app` — author files directly. Phase 2 builds actual pages.

First create the directory structure:
```
apps/web/
  app/
    page.tsx
    layout.tsx
  package.json
  next.config.ts
  tsconfig.json
```

**apps/web/package.json**:
```json
{
  "name": "@sip/web",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "@sip/core": "workspace:*",
    "next": "15.3.2",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "@types/node": "22.15.3",
    "@types/react": "19.1.4",
    "@types/react-dom": "19.1.2",
    "typescript": "5.8.3"
  }
}
```
Use exact versions (no `^` or `~`). Verify current stable versions of next/react if the above seem stale — these are current as of 2026-05-06.

**apps/web/next.config.ts**:
```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  transpilePackages: ["@sip/core"],
};

export default nextConfig;
```
`transpilePackages` ensures Next.js compiles the workspace TypeScript source directly without requiring a build step for @sip/core in dev.

**apps/web/tsconfig.json**:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

**apps/web/app/layout.tsx**:
```typescript
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
```

**apps/web/app/page.tsx**:
```typescript
export default function Home() {
  return <div />;
}
```
This is intentionally empty — Phase 2 builds the actual UI.
  </action>
  <verify>
    <automated>test -f /c/Users/navne.FUDGE/source/sip-calculator/apps/web/package.json && grep -c '"@sip/core": "workspace:\*"' /c/Users/navne.FUDGE/source/sip-calculator/apps/web/package.json</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/package.json exists and contains `"@sip/core": "workspace:*"`
    - apps/web/package.json contains `"next": "15.3.2"` (exact version, no caret)
    - apps/web/next.config.ts exists and contains `transpilePackages`
    - apps/web/tsconfig.json exists
    - apps/web/app/page.tsx exists and contains `export default function Home`
    - apps/web/app/layout.tsx exists and contains `export default function RootLayout`
  </acceptance_criteria>
  <done>apps/web stub created with 5 files. `@sip/core: workspace:*` dependency declared. Phase 2 can start without any additional scaffold work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local disk | Package manifests downloaded during `pnpm install` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | package.json, apps/web/package.json | mitigate | Pin exact versions (no `^` or `~`) in all package.json files. Supply chain attack surface is proportional to version range width. |
| T-01-01-02 | Information Disclosure | .npmrc | accept | .npmrc contains no secrets — only boolean flags. Low risk. |
</threat_model>

<verification>
After both tasks complete:
- `node -e "const t=require('./turbo.json'); if(!t.tasks) throw new Error('missing tasks'); console.log('OK')"` exits 0
- `grep -c '"tasks"' turbo.json` returns 1
- `grep -c 'apps/\*' pnpm-workspace.yaml` returns 1
- `grep -c '"@sip/core": "workspace:\*"' apps/web/package.json` returns 1
</verification>

<success_criteria>
Monorepo root is configured with Turbo 2.x task pipeline. apps/web stub exists with @sip/core workspace dependency declared. A developer running `pnpm install` from the root will resolve the workspace graph correctly once packages/core is created in Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/01-calculation-engine/01-01-SUMMARY.md` following the summary template.
</output>
